// TODO: caracteres

export const route1 = "GET / v1 / public / characters";
// Obtiene listas de caracteres.

export const route2 = "GET / v1 / public / characters / { characterId }";
// Obtiene un solo carácter por id.

export const route3 =
  "GET / v1 / public / characters / { characterId } / comics";
// Obtiene listas de historietas filtradas por una identificación de personaje.

export const route4 =
  "GET / v1 / public / characters / { characterId } / events";
// Obtiene listas de eventos filtrados por una identificación de personaje.

export const route5 =
  "GET / v1 / public / characters / { characterId } / series";
// Obtiene listas de series filtradas por una identificación de personaje.

export const route6 =
  "GET / v1 / public / characters / { characterId } / stories";
// Obtiene listas de historias filtradas por una identificación de personaje.

// TODO: comics

export const route7 = "GET / v1 / public / comics";
// Obtiene listas de cómics.

export const route8 = "GET / v1 / public / comics / { comicId }";
// Obtiene un solo cómic por id.

export const route9 = "GET / v1 / public / comics / { comicId } / characters";
// Obtiene listas de personajes filtrados por una identificación cómica.

export const route10 = "GET / v1 / public / comics / { comicId } / creators";
// Obtiene listas de creadores filtrados por una identificación de cómic.

export const route11 = "GET / v1 / public / comics / { comicId } / events";
// Obtiene listas de eventos filtrados por una identificación cómica.

export const route12 = "GET / v1 / public / comics / { comicId } / stories";
// Obtiene listas de historias filtradas por una identificación de cómic.

// TODO: creadores

export const route13 = "GET / v1 / public / creators";
// Obtiene listas de creadores.

export const route14 = "GET / v1 / public / creators / { creatorId }";
// Obtiene un solo creador por id.

export const route15 = "GET / v1 / public / creators / { creatorId } / comics";
// Obtiene listas de historietas filtradas por una identificación de creador.

export const route16 = "GET / v1 / public / creators / { creatorId } / events";
// Obtiene listas de eventos filtrados por una identificación de creador.

export const route17 = "GET / v1 / public / creators / { creatorId } / series";
// Obtiene listas de series filtradas por una identificación de creador.

export const route18 = "GET / v1 / public / creators / { creatorId } / stories";
// Obtiene listas de historias filtradas por una identificación de creador.

// TODO: eventos

export const route19 = "GET / v1 / public / events";
// Obtiene listas de eventos.

export const route20 = "GET / v1 / public / events / { eventId }";
// Obtiene un solo evento por id.

export const route21 = "GET / v1 / public / events / { eventId } / characters";
// Obtiene listas de caracteres filtrados por una identificación de evento.

export const route22 = "GET / v1 / public / events / { eventId } / comics";
// Obtiene listas de historietas filtradas por una identificación de evento.

export const route23 = "GET / v1 / public / events / { eventId } / creators";
// Obtiene listas de creadores filtrados por un ID de evento.

export const route24 = "GET / v1 / public / events / { eventId } / series";
// Obtiene listas de series filtradas por un ID de evento.

export const route25 = "GET / v1 / public / events / { eventId } / stories";
// Obtiene listas de historias filtradas por una identificación de evento.

// TODO: series

export const route26 = "GET / v1 / public / series";
// Obtiene listas de series.

export const route27 = "GET / v1 / public / series / { seriesId }";
// Obtiene una sola serie de historietas por id.

export const route28 = "GET / v1 / public / series / { seriesId } / characters";
// Obtiene listas de caracteres filtrados por una identificación de serie.

export const route29 = "GET / v1 / public / series / { seriesId } / comics";
// Obtiene listas de historietas filtradas por una identificación de serie.

export const route30 = "GET / v1 / public / series / { seriesId } / creators";
// Obtiene listas de creadores filtrados por una identificación de serie.

export const route31 = "GET / v1 / public / series / { seriesId } / events";
// Obtiene listas de eventos filtrados por una identificación de serie.

export const route32 = "GET / v1 / public / series / { seriesId } / stories";
// Obtiene listas de historias filtradas por una identificación de serie.

// TODO: historias

export const route33 = "GET / v1 / public / stories";
// Obtiene listas de historias.

export const route34 = "GET / v1 / public / stories / { storyId }";
// Obtiene una sola historia de cómic por id.

export const route35 = "GET / v1 / public / stories / { storyId } / characters";
// Obtiene listas de personajes filtrados por una identificación de historia.

export const route36 = "GET / v1 / public / stories / { storyId } / comics";
// Obtiene listas de historietas filtradas por una identificación de historia.

export const route37 = "GET / v1 / public / stories / { storyId } / creators";
// Obtiene listas de creadores filtrados por una identificación de historia.

export const route38 = "GET / v1 / public / stories / { storyId } / events";
// Obtiene listas de eventos filtrados por una identificación de historia.

export const route39 = "GET / v1 / public / stories / { storyId } / series";
// Obtiene listas de series filtradas por una identificación de historia.
